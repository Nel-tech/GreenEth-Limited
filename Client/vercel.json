{
  "buildCommand": "pnpm install --ignore-workspace && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "engines": {
    "node": "20.x"
  }
}